Drop screenshots here and reference them from the main README, e.g.:

- `list-view.png` — the article list (sidebar + article rows)
- `reader-view.png` — an article open in the reading pane

Keeping this as a placeholder until real screenshots are added — the
theme's had a few CSS fixes since the last round of screenshots were
taken, so those weren't included as-is to avoid documenting a stale state.
